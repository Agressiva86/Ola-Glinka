---
const { 
    content,
    name,
    dog
} = Astro.props;
---

<div
    class="flex flex-col bg-lightbg-400 border border-accent-500 shadow-2xs rounded-[30px] dark:border-dark-600/30 dark:bg-dark-600/30 gap-6 p-10 lg:hover:shadow-2xl/20 lg:hover:shadow-secondary-700 lg:dark:shadow-none transotion-all duration-300"
>
    <div class="flex-auto">
        <p
            class="text-base text-primary-500 dark:text-accent-100 leading-relaxed"
        >
            <em>
                {content}
            </em>
        </p>
    </div>

    <div class="">
        <h3
            class="text-sm font-heading font-semibold text-primary sm:text-base dark:text-accent"
        >
            {name}
        </h3>
        <p class="text-sm text-secondary dark:text-accent-200">
            {dog}
        </p>
    </div>
</div>
<!-- End Card -->